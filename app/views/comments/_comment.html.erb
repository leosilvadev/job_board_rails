<div>
	<h4>
		<%= comment.name %> said:
	</h4>
	<%= simple_format comment.body %>
	Sent <b><%= time_ago_in_words comment.created_at %></b> ago
	<%= link_to "Destroy", comment_path(comment), method: :delete, data: {confirm: 'Are you sure?'} %>
</div>